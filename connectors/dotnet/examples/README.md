# .NET Examples

- [`ConsoleApp`](./ConsoleApp): minimal console app showing connector setup,
  manual capture, and optional hook installation.

Set `AURAKEEPER_ENDPOINT` and `AURAKEEPER_API_TOKEN` before running against a
real AuraKeeper environment.
